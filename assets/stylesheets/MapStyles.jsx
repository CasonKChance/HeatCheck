import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mapContainer: {
    flexDirection: 'row',
    margin: 20,
    marginTop: 50,
    marginBottom: 10,
    height: 250,
  },
  map: {
    flex: 1,
  },
});

export default styles;
